import { ChangeCusorDirective } from './change-cusor.directive';

describe('ChangeCusorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeCusorDirective();
    expect(directive).toBeTruthy();
  });
});
